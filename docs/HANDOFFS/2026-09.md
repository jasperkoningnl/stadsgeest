# Technische overdrachten — september 2026

## 2026-09-12 — Claude-context vervangen door progressieve Codex-context

**Resultaat:** de actieve documentatie is naar de applicatierepository gebracht.
`AGENTS.md`, `docs/INDEX.md` en `docs/CURRENT.md` vormen het standaard
startpakket. De oude `STATUS.md` en `CLAUDE.md` zijn ongewijzigd gearchiveerd.
Een korte statusverwijzer, taakgerichte leesroutes, runbooks, faseplan en
redactioneel profiel zijn toegevoegd.

**Verificatie:** de documentatiecontrole slaagt; 58 offline tests slagen; lint
heeft geen fouten en één bestaande waarschuwing; de productiebuild slaagt. De
werkset is alleen-lezen tegen Turso gecontroleerd en de schrijfhelper doorliep
een geldige dry-run zonder writes.

**Open risico's:** de oude Claude-weger moet uitgeschakeld zijn voordat de
gepauzeerde Codex-taak wordt geactiveerd. Lokale, niet-gevolgde detectie- en
onderwijsinspectiebestanden worden niet automatisch meegenomen.

**Volgende stap:** Claude/Cowork uitschakelen, daarna de Codex-taak activeren en
de eerste productierun inhoudelijk beoordelen.

## 2026-09-20 — Dagelijkse detectie en fase-1-bronnen hersteld

**Resultaat:** de actuele Belastingdienst-ANBI-XML wordt verwerkt; GLEIF gebruikt
de ondersteunde fulltextzoekopdracht met exact lokaal adresfilter en vraagt
alleen expliciet aanwezige parentrelaties op; OSM gebruikt de juiste
gemeenterelaties en geldige bronmetadata. Dode proceslocks worden direct
opgeruimd. De gecombineerde dagelijkse run gebruikt 1,5 GB heap in plaats van
de te krappe 768 MB.

**Verificatie:** ANBI baseline 707 records; GLEIF baseline en herhaling 2.073
records, nul wijzigingen/events; OSM baseline en herhaling 15.521 records, nul
wijzigingen/events. De volledige detectierun doorliep alle zestien regels zonder
fouten. De echte Windows-taak `Stadsgeest Detection` eindigde daarna met code 0;
`Stadsgeest NDW` eveneens.

**Open risico's:** fase 0–2 zijn formeel nog niet gesloten. Golden-setbewijs,
BAG-koppeling, merge/unmerge en vijf aantoonbaar graph-gevonden signalen missen
nog als sluitbewijs. Fase 5 blijft observationeel open.

**Volgende stap:** voer de formele slotaudits van fase 0, 1 en 2 uit en leg per
ontbrekend onderdeel uitvoering of een expliciete afwijkingsbeslissing vast.

## 2026-09-20 — Slotaudits fase 0 en 2; fase 1 beveiligd

**Resultaat:** fase 0 slaagt met herleidbare oorspronkelijke en actuele
baseline, 75.935 herverwerkbare bronrecords en nul KOOP-dubbelingen. Het
graph-exitbewijs van fase 2 slaagt met 37 graph-afhankelijke replayhits en nul
matches in de contraproef zonder graph. Merge/review/unmerge is transactioneel
en geaudit; zes lokale kernseeds hebben bron-, reden- en reviewmetadata.

**Open:** fase 1 wacht op BAG-backfill (0/82) en menselijke labeling van 200 uit
250 voorbereide organisatiekandidaten. De BAG-afhankelijkheid houdt ook het
vergunningonderdeel en daarmee de formele sluiting van fase 2 open. Fase 5 heeft
14 rollende beoordelingen, zes bruikbare tips en één nog open augustus-review;
effectbewijs blijft te klein.

## 2026-09-21 — BAG- en vergunningkoppeling afgerond

**Resultaat:** PDOK gaf voor 79 van 82 bestaande locaties een exacte BAG-match.
Drie niet-eenduidige adressen zijn gemotiveerd als uitzondering vastgelegd. Zes
bestaande vergunningrecords zijn exact aan zo'n BAG-locatie en bekende lokale
organisatie gekoppeld; de identieke herhaling maakte nul duplicaten. Fase 2 is
hiermee formeel gesloten.

**Open:** fase 1 wacht alleen nog op menselijke labeling van 200 van de 250
voorbereide organisatiekandidaten. Fase 5 blijft observationeel open.

## 2026-09-23 — NER spoor 1: spaCy-vermeldingen naast de aliasextractie

**Resultaat:** `extract-ner.cjs` (Node, alle I/O) en `ner/spacy_worker.py`
(spaCy `nl_core_news_lg`, geen DB of netwerk) schrijven naar de nieuwe
additieve tabellen `document_mentions` en `ner_scans`. Backfill: 3.050 items,
10.912 vermeldingen, 753 persoons- en 261 organisatiekandidaten. KG-tellingen
voor en na gelijk (589/767/968). Tweede run op dezelfde 50 items schreef 0
rijen. De weger-werkset heeft een veld `ner_kg_kandidaten`; `WEGER.md` zegt hoe
de weger dat moet lezen. `npm test` (258) en de documentatiecontrole slagen.

**Gevonden:** het evaluatierapport van 21-9 bevatte fouten (te kleine
steekproef met dubbele teksten, verzonnen confidence-waarden, onjuist
migratievoorstel omdat `entity_mentions` naar `entities` wijst en niet naar
`kg_entities`); gecorrigeerd in de projectdocumentatie. Nextdoor en andere
sociale bronnen leverden namen van burgers en veel ruis op en zijn uitgesloten.
Organisatieprecisie is laag op B&W-tekst (25% in de validatieset); personen via
de KG zijn de duidelijke winst, vooral afgekorte portefeuillehouders.

**Open:** planning (voorstel: extra stap in `run-detection-task.ps1`, besluit
Jasper), reviewingang in het dashboard, items met id < 5187, en een nieuwe
meting van filter-7. De Python-venv staat buiten de repo in
`%LOCALAPPDATA%\stadsgeest-ner`; zonder die venv faalt de runner met een
duidelijke melding. Er zijn geen offline tests voor het ruisfilter, omdat
spaCy niet in de testomgeving zit.

## 2026-09-23 — Weger op achterstand en verbanden gericht

**Resultaat:** `operations/WEGER.md` is op vijf punten aangepast:
- werkset met `--limit 25`, met een tweede batch als de eerste vol is;
- een snelle triage van routinesignalen naar `discarded`;
- een verplichte verbandencheck (sectie 3a): adres via BAG en monumenten,
  organisatie en persoon in de registers, met geverifieerde sleutels;
- twee nieuwe scoreposten (`kruisbronverband` +3, `alleen_naamovereenkomst` −3)
  en hoogstens drie tips per run;
- een sweep over de hele database als de werkset geen tip oplevert (sectie 3b).

De eis van minimaal één tip per run blijft (keuze van Jasper). Een dunne dagtip
komt pas na een sweep en mag geen routine, los incident, niet-lokaal verhaal of
bekend nieuws zijn. De hulpscripts staan in de repo:
`scraper/src/weger-adres.cjs` (PDOK/BAG, buurtcode, rijksmonumenten binnen
10 m) en `scraper/src/weger-query.cjs` (alleen-lezende SQL), met tests in
`__tests__/weger/hulpmiddelen.test.cjs`.

Aanleiding is een analyse van 9 tot en met 23 september:
- de weger las vast tien signalen per dag, grotendeels routine;
- 198 signalen zijn nooit gelezen;
- er was geen enkel signaal met meerdere bronnen;
- de registers in `source_records` werden niet geraadpleegd. Voorbeeld: de
  aanvraag voor Nieuweweg 4 (rijksmonument 8044) werd afgewezen met "noemt geen
  monumentstatus".

**Verificatie:** `npm --prefix scraper test` slaagt (261 tests). Handmatig:
Nieuweweg 4 geeft monument 8044, Langestraat 117 terecht geen (monument 121 is
het buurpand). `weger-query.cjs` weigerde `delete` en `with … delete`. Er is
nog geen weger-run gedraaid met de nieuwe tekst.

**Open risico's:** de koppellaag is nog handwerk van de weger, geen code. Er
zijn geen gedeelde sleutels in de database, en het insolventieregister staat
sinds 28 mei stil.

**Volgende stap:** de eerste run met de nieuwe tekst beoordelen op de
weggegooide routinesignalen en de gerapporteerde koppelgaten.

## 2026-09-23 — NER ingepland; stderr brak de dagelijkse taken af

**Gevonden:** `Stadsgeest Detection` eindigde op 23-9 om 06.15 uur met code 1.
De detectierun stopte midden in Asbest en evaluatie en retentie liepen niet.
Oorzaak: Windows PowerShell 5 maakt met `$ErrorActionPreference = 'Stop'` van
elke stderr-regel van `node` een fatale fout. Nagebootst en bevestigd.
`run-ndw-task.ps1` had hetzelfde latente probleem.

**Resultaat:** beide taakscripts draaien de native stappen nu met
`Continue` en tellen alleen exitcodes; stderr komt als gewone tekst in het log.
`run-detection-task.ps1` draait als laatste stap `extract-ner.cjs --limit 500`.
De Python-venv is verhuisd naar `scraper\.ner-venv`: de eerste venv in
`%LOCALAPPDATA%` stond in de gevirtualiseerde pakketmap van de Claude-app en
was voor de Taakplanner onzichtbaar (ENOENT bij de eerste geplande test).

**Let op:** een handmatige start van de taak terwijl NDW draait, stuit op de
detectielock (NDW om :08/:23/:38/:53). Om 06.15 uur speelt dat niet.

## 2026-09-23 — RaadKijker als bron voor moties en amendementen

**Gevonden:** bron 116 (Moties) kreeg sinds 15 juli niets meer binnen. Van de
84 moties sinds 1 juni bij RaadKijker ontbraken er 38 in `raw_items`, en 19
aanwezige hadden geen fulltext. RaadKijker levert voor Amersfoort een lijst met
indiener en Notubiz-link, maar geen stemgedrag. Het veld `uitslag` klopt niet
altijd (2026-054M). `api.notubiz.nl/document/…` geeft voor een deel van de
nieuwste stukken HTTP 400, oudere PDF's komen gewoon binnen.

**Resultaat:** `raadkijker-moties.js` in `run-all.js`, regels in `SOURCES.md`.
Eerste run: 7 nieuwe moties van 23-9 onder 116. Na drie runs zijn 22 bestaande
moties aangevuld met fulltext. Het wegschrijven is idempotent: runs 2 en 3
leverden 0 nieuwe items.

**Verificatie:** `node --test __tests__/raadkijker/raadkijker.test.mjs` (5
tests). Dry-run en drie echte runs, telkens 13 tot 19 seconden. De rijen zijn
in Turso nageteld en `scrape_runs` staat op job `handmatig-raadkijker`.

**Open risico's:**

- De test staat nog niet in `npm test`, omdat `scraper/package.json` openstaand
  werk van een andere sessie bevat.
- Negen stukken blijven HTTP 400 geven en worden dagelijks opnieuw geprobeerd.
- Een late uitslag geeft geen nieuw signaal.
- De `run-all`-run zelf (timeout van 60 seconden) is nog niet gezien.

**Volgende stap:** de eerste geplande `run-all` controleren in `scrape_runs`.
Daarna besluiten of een late uitslag een eigen signaal moet worden.

## 2026-09-23 - OpenKvK via overheid.io als dagelijkse KG-adapter

**Resultaat:** `openkvk-register.cjs` staat in de detectierun (`openkvk`) en
haalt per dag de registerwijzigingen van de vorige dag op voor Amersfoort en
Leusden: twee calls per dag. Een volledige baseline is onmogelijk (hooguit 10
pagina's per zoekopdracht, geen sortering, geen inschrijfdatum), dus de adapter
werkt als incrementele feed met een dagcursor. R9 kent drie nieuwe
`KVK_REGISTRATION_*`-types. De oude lege stub in `run-nieuw.js` is weg, en
daarmee ook bron 80 met zijn 16 lege `scrape_runs`-regels. Sleutel
`OVERHEID_IO_KEY` in `scraper/.env`.

**Verificatie:** 15 nieuwe tests, volledige scrapertestset 288/288 groen. Dry-run
en echte eerste run op 22 september: 2 calls, 40 records, 15 organisaties met
KVK-identifier, 0 events (opwarmperiode). Een tweede run kost 0 calls. Tot nu
toe zijn er ongeveer 12 calls van het tegoed verbruikt, inclusief de verkenning.

**Open risico's:**

- overheid.io zegt niet per welke periode de 2.500 calls gelden.
- Het signaalvolume na 7 oktober is nog niet gemeten.
- De test staat niet in `npm test`, omdat `scraper/package.json` openstaand werk
  van een andere sessie bevat.

**Volgende stap:** op 8 oktober de eerste `KVK_REGISTRATION_*`-events en de
R9-signalen natellen. Is dat meer dan een handvol per dag, dan de
rechtsvormfilter aanscherpen.

## 2026-09-23 — Insolventies, TenderNed-winnaars en Woo-bijlagen

**Gevonden:** bron 48 (Centraal Insolventieregister) had nooit een scraper en
nul items, terwijl `sources` hem als actief toonde. TenderNed-gunningen gaven de
winnaar alleen als tekst in `content`, zonder KvK of adres. Van Woo-besluiten
op iBabs werden de bijlagen (besluit, inventarislijst, openbaar gemaakte
stukken) nooit gelezen. Het CIR heeft een openbare dag-API
(`BekendmakingenService/getAll` en `haalOp/{dag}`, ±1 maand terug). De
zoekservices van het CIR hebben anti-CSRF; die gebruiken we niet.

**Resultaat:**

- `scrapers/insolventies.js` (+ `insolventies-lib.js`) in `run-all.js`: alleen
  rechtspersonen met een vestigingsadres in Amersfoort/Leusden. Natuurlijke
  personen (geb., woonadres), eenmanszaken en schuldsaneringen worden bewust
  niet opgeslagen.
- `tenderned-partijen.js` leest hoofdstuk 8 (Organisaties) uit de eForms-PDF
  naar de nieuwe tabel `tender_parties` (koper, winnaar, KvK, adres, waarde).
  `tenderned.js` schrijft dit dagelijks mee. `backfill-tenderned-partijen.js`
  haalt ~2.350 gegunde opdrachten sinds maart 2024 op (zoektermen amersfoort,
  leusden) en schrijft niet naar `raw_items`.
- `scrapers/ibabs-bijlagen.js` (+ `ibabs-lib.js`) in `run-weekly.js`: PDF's van
  Woo-besluiten en convenanten naar de nieuwe tabel `raw_item_attachments`, en
  samengevoegd in `raw_items.full_text` (daarbij wordt `entities_scanned_at`
  leeggemaakt). Oude items die nog niet in `raw_items` stonden, krijgen
  `is_historical=1` en `is_processed=1`: ze voeden de extractie, maar maken
  geen signalen.

**Verificatie:** 12 tests in `__tests__/insolventies`, `__tests__/tenderned`
en `__tests__/ibabs`. De CIR-run schreef 3 items (Houtindustrie Amersfoort,
ASD Transport, Kinetic Evaluation Instruments), nageteld in Turso. De
TenderNed-dagrun schreef 2 partijrijen. De backfills liepen bij de commit nog
op de notebook (log in `scraper/tmp/`).

**Open risico's:**

- De tests staan niet in `npm test`, omdat `scraper/package.json` openstaand
  werk van een andere sessie bevat.
- Scans zonder tekstlaag krijgen status `geen_tekst`; OCR doen we niet.
- De koper- en winnaarsrollen in een TenderNed-PDF zijn soms fout ingevuld door
  de publicerende partij (voorbeeld: publicatie 333160).
- De bronnenwacht meldde bron 48 niet als stil, omdat er nooit runs waren.

**Volgende stap:** `tender_parties` (winnaars met KvK) en de insolventies
meenemen in de Splink-koppeling van `scraper/tmp/splink-proef/`.

## 2026-09-23 — Adressen exact aan de BAG gekoppeld

**Resultaat:** `extract-addresses.cjs` en `link-register-addresses.cjs` koppelen
adressen met huisnummer via de PDOK Locatieserver aan de BAG-nummeraanduiding
(zie `ADRESKOPPELING.md`). Backfill: 6.970 items gescand, 1.540 adresrijen,
waarvan 1.325 exact, verdeeld over 1.194 items. 221 adressen komen in meer dan één
item voor. Registers: LRK 316/316 exact, DUO 82/86, GLEIF 2.033/2.068. 79
adressen uit documenten staan ook in een register, 15 bij een KG-locatie. In de
werkset krijgen 115 van de 288 open signalen met een exact adres een
`adres_koppelingen`-blok. Twee steekproeven van samen 24 exacte koppelingen
waren allemaal juist. 268 tests en de documentatiecontrole slagen.

**Gevonden:** één document (parkeerrestrictielijst, 2.378 adressen) hield de
eerste backfill minutenlang bezig; documenten met meer dan 50 adressen worden nu
alleen geteld. Kadastrale aanduidingen ('M 431, Amersfoort') en huisnummerreeksen
('69-71') gaven valse of gemiste treffers; beide zijn afgevangen. Kamp Amersfoort
ligt volgens de BAG in Leusden, dus 'Loes van Overeemlaan 19 te Amersfoort'
koppelt terecht niet. GLEIF bevat persoonlijke holdings op woonadressen; een
vergunning op zo'n adres noemt dan indirect de bewoner.

**Open:** rijksmonumenten (alleen coördinaten; BAG-adres uit het RCE-register
nodig), OSM, UIT-agenda, zorg en asbest; BAG-pand-id (vereist BAG API-sleutel);
een eerste backfillrun hing op een Turso-verzoek zonder time-out en is handmatig
herstart.

## 2026-09-23 — Bronnenwacht ziet bronnen zonder runs

**Gevonden:** de bronnenwacht sloeg elke bron met minder dan 6 runs over en liet
die op `ok` staan. Een actieve bron zonder scraper leek daardoor gezond; zo
stond bron 48 (CIR) maandenlang op `ok`. De heartbeat filterde bovendien op
`scrape_runs.created_at`, een kolom die niet bestaat. Die query faalde altijd,
dus de waarschuwing is nooit afgegaan. `subsidieregister-records.js` logde
geen runs, omdat de records in `subsidies` terechtkomen en niet in `raw_items`.

**Resultaat:**

- Actieve bronnen met cadans hourly/daily/weekly die 30 dagen geen
  `scrape_runs`, geen `fetch_runs` en geen nieuw `raw_item` hebben, krijgen
  health `geen_runs` met een toelichting. Zodra er weer iets draait, gaat de
  status terug naar `ok`.
- De heartbeat gebruikt `started_at` en schrijft naar het dagrapport in plaats
  van naar `STATUS.md` (dat is nu alleen een verwijzer).
- Subsidieregister logt nu een run via `lib.log`.

**Verificatie:** een handmatige run gaf 14 bronnen met `geen_runs`. Na een run
van het subsidieregister (1.678 records, gelijk aan de stand ervoor) stond bron
26 weer op `ok`. CIR (48), iBabs (35) en TenderNed (132) staan op `ok`.

**Open risico's:** de 14 gemarkeerde bronnen zijn echte restanten: dubbele
oude rijen (7, 31, 41, 108), bronnen zonder scraper (6, 10, 49, 50, 52),
vervangen bronnen (18, 51) en stilgevallen bronnen (9, 113, 114). Uitzetten of
repareren is een keuze voor Jasper. Het dashboard toont `geen_runs` als rode
badge zonder eigen filterpil.

**Volgende stap:** per gemarkeerde bron kiezen: uitzetten, samenvoegen of
repareren.

## 2026-09-24 — Vier dubbele bronrijen uitgezet

**Resultaat:** de bronrijen 7 (TenderNed-RSS, vervangen door 132), 41 (B&W via
de oude Notubiz-url, vervangen door 131) en 31 en 108 (oude raadsinformatie,
vervangen door de ORI-stromen 115-119) staan nu op `is_active=0` en
`health='dubbel'`, met een verwijzing in `health_note`. Hun 218 items blijven
als historie bewaard; er zijn geen items verplaatst. `getOrCreateSource` zoekt
op url, dus de lopende scrapers blijven naar 131, 132 en 115-119 schrijven. De
bronnenwacht beoordeelt voortaan alleen actieve bronnen. Anders zou een
uitgezette rij met veel historische runs (7 had er 76) weer als `dood`
gemarkeerd worden.

**Verificatie:** na de bronnenwacht-run staan 7, 31, 41 en 108 nog op
`dubbel`. Er zijn 128 actieve bronnen gecontroleerd en 10 staan op `geen_runs`
(6, 9, 10, 18, 49, 50, 51, 52, 113, 114).

**Open risico's:** geen.

**Volgende stap:** Jasper kiest per `geen_runs`-bron: bouwen, uitzetten of
vervangen.

## 2026-09-24 — Monumenten, meer registers, BAG-pand en NER-restpunten

**Resultaat:** de dagelijkse taak van 24-9 06.15 uur eindigde met code 0 en
draaide NER (500 items) en adreskoppeling (177 items) mee. Registerkoppeling
uitgebreid met zorgverantwoording (187 exact), OSM (3.140), UIT-agenda (39
locaties), asbestovertredingen (0 lokaal) en rijksmonumenten (803 van 849
exact). Monumenten komen live uit het RCE-register (SPARQL,
`heeftBAGRelatie`); `source_records` bevat alleen coördinaten. 48
monumentadressen komen ook in een document voor, onder meer Nieuweweg 4 (het
signaal dat de weger op 23-9 afwees omdat de publicatie geen monumentstatus
noemde). `link-bag-panden.cjs` haalt via de PDOK BAG OGC API (zonder sleutel)
pand-id, bouwjaar en aantal verblijfsobjecten op; de werkset toont
`zelfde_pand_ander_adres` bij panden met hoogstens 20 verblijfsobjecten.
NER-restpunten: kandidaat gelijk aan de bron ('NS') valt weg uit de werkset;
`stilte-alarm.mjs` bewaakt `ner_scans` en `address_scans` (48 uur);
`ner-review.cjs` exporteert en importeert een CSV-steekproef voor handmatige
controle (eerste steekproef van 60 staat in Stadsgeest-documentatie).

**Gevonden:** hermeting filter-7 op 250 willekeurige items: organisaties 48%,
locaties 68%. Verder filteren loont niet; onopgeloste organisaties blijven
buiten de weger. RCE koppelt soms een nummeraanduiding-id waar een
verblijfsobject-id hoort te staan; daarom wordt het monumentadres opnieuw via
postcode en huisnummer bij PDOK opgezocht. Asbestovertredingen liggen vrijwel
allemaal buiten de gemeente.

**Open:** een beoordeling van de NER-steekproef door Jasper; monumenten
zonder BAG-relatie (muren, bruggen) koppelen niet.

## 2026-09-24 — Review 23-9, gastouders uit adreskoppeling, tests in npm test

**Resultaat:**

- Gastouders (LRK-type VGO) worden niet meer aan adressen gekoppeld; 86 rijen
  uit `register_addresses` verwijderd (LRK nu 230 rijen). Zie `DECISIONS.md`.
  Het filter in `link-register-addresses.cjs` is meegegaan in commit c5986b3.
- `npm test` bevat nu ook OpenKvK, RaadKijker, insolventies, TenderNed-partijen,
  iBabs en de detection-engine-test die sinds 5-9 ongevolgd in de map stond:
  312 tests, alle groen (was 268).

**Verificatie:** dry-run `link-register-addresses.cjs --only lrk` geeft 230
records; tellingen voor en na het verwijderen: VGO 86 → 0, LRK 316 → 230.

**Gevonden bij de review van 23-9:**

- Steekproef van de 316 signalen die de verbandenrun van 23-9 in één plan
  afwees: 20 willekeurig plus 15 uit de 111 die niet onder een routineregel
  vielen. 33 terecht, 2 twijfelachtig maar geen gemiste tip: 2334 (oproep Volt
  Zeist over partijsubsidies, afgedaan als "niet lokaal", terwijl motie
  2026-101M over hetzelfde onderwerp gaat) en 2413 (De Kamp autoluw, had als
  dossierfeit bij het masterplan binnenstad gekund). De adressen in de
  verbandencheck-redenen bevatten ruis uit de brontekst (andere adressen dan
  het perceel).
- Signaalaanmaak 24-9: 41, boven de bandbreedte. Vooral routine uit
  Officiële Bekendmakingen; de nieuwe bronnen (moties, CIR, TenderNed) voegen
  op raadsdagen 5 à 10 toe. Er staan 50 signalen "Vertrokken met onbekende
  bestemming van [naam]" (BRP, particulieren), waarvan 40 op watching.
- OpenKvK: na de opwarmperiode wordt vrijwel elke stichting of vereniging die
  voor het eerst in de wijzigingen staat als nieuw gemeld. Elke BV krijgt een
  `kg_entities`-rij (15 op 23-9, 21 op 24-9). `knownKvk` kiest een willekeurige
  identifier-rij.
- `document_mentions` groeit met ongeveer 4.800 rijen per dag, waarvan de
  helft onopgeloste locaties die niemand leest.
- RaadKijker heeft ook Leusden (cbs 0327, `gemeente=leusden`), onder meer de
  twee moties van 23-9, maar met data_level `alleen_documenten`.

**Open:**

- De hulpscripts van de verbandenzoeker in `stadsgeest-werk` (buiten de repo)
  lezen het LRK mogelijk nog rechtstreeks, inclusief gastouders; niet
  gecontroleerd.
- Besluiten van Jasper over OpenKvK ("nieuw" via KvK-nummer), een intakefilter
  voor BRP-bekendmakingen en de bronnen zonder runs.

## 2026-09-24 — Samenvoeging De Alliantie en rommel in toezichtrelaties

**Gedaan:**

- KG #573 (Stichting de Alliantie, van de RvC-pagina, zonder KvK) samengevoegd
  in #137 (KvK 39048769), op verzoek van Jasper na de NER-controle. Kandidaat 1,
  audit 1; terug te draaien met `node manage-entity-merge.cjs unmerge 1`.
  #137 heeft nu 14 relaties. Vermelding 7376 staat op candidate -> 137.

**Gevonden:**

- De tien TOEZICHTHOUDER-relaties die van #573 zijn meegekomen (relaties
  137-146, personen #574-#583) zijn parserrommel: `extractGovernanceFacts` in
  `phase4-context-sources.cjs` leest nevenfuncties als namen ("Voorzitter Raad",
  "Governance Code", "Voorzitter Proteus"). De echte commissarissen stonden al
  als FUNCTIE op #137. Zelfde soort rommel bij Portaal: #572 "Reglement Raad"
  (relatie 136) en afgekapt "Jan Maarten" (#567).
- `ensureGovernanceRelation` zoekt de organisatie op naam zonder
  `merged_into_id` te volgen; bij de volgende run komen nieuwe relaties op het
  samengevoegde #573. De detectieregels gebruiken deze relatietypen.

**Open (wacht op Jasper):** rommelrelaties beëindigen, lookup laten volgen op
`merged_into_id`, parser repareren. Niets daarvan is uitgevoerd.

## 2026-09-24 — Organisatiekoppeling met Splink in productie

**Resultaat:** `koppel-organisaties.cjs` + `koppel/` (bronnen, normalisatie,
Splink-worker) koppelen organisaties uit 18 bronnen, waaronder de nieuwe
TenderNed-winnaars, CIR-insolventies en OpenKvK. Eerste run (run 1): 6.901
records, 199 kruisclusters in `org_clusters`. De werkset heeft per signaal
`organisatie_verbanden` en bovenaan `kruisbron_kandidaten`; `WEGER.md` zegt hoe
de weger ze leest. Uitleg in `docs/KOPPELING.md`, besluit in `DECISIONS.md`.

**Verificatie:** 9 nieuwe tests (`__tests__/koppel`), `npm test` 322 groen,
documentatiecontrole geslaagd. Tweede run met dezelfde invoer: "Invoer
ongewijzigd", niets geschreven. Telling na schrijven gelijk aan het aantal
records (6.901). Steekproef van 23 kruisclusters: alle juist. Werkset op 297
signalen van de laatste tien dagen: 7 met een organisatieverband, 1 via exact
KvK, ongeveer 180 ms per signaal.

**Gevonden:** met alleen de Splink-kans gingen bedrijven in hetzelfde
verzamelgebouw samen, en koppelde een KG-entiteit zonder plaats De Baander in
Amersfoort aan De Baander in Elim. Daarom gelden nu harde regels (naam moet
lijken, KvK- en plaatsconflict blokkeren). Het rendement is klein: maar 2
clusters combineren geld en toezicht, en beide zijn niet lokaal. De koppeling
werkt; het knelpunt blijft dat toezichtbronnen weinig lokale organisaties
bevatten en dat personen (bestuurders) ontbreken.

**Open:**

- De stap staat nog niet in `run-detection-task.ps1`: dat bestand had
  ongecommit werk van een andere sessie (logcodering). Toe te voegen na de
  pandkoppeling: `$koppelExit = Invoke-NodeStep @('src\koppel-organisaties.cjs')`
  en `$koppelExit` in de exitcontrole. Tot dan draait de koppeling alleen
  handmatig.
- Aanbeveling: bestuurders (ANBI-jaarverslagen, governance) als volgende bron;
  daar lopen de verrassende verbanden.

## 2026-09-24 — Leusden: vergaderstukken via Notubiz, moties via RaadKijker

**Gevonden:** bron 129 (ORI Leusden) staat op 'dood' omdat de ORI-index stopt
bij de raad van 9 juli 2026. Leusden is na de zomer overgestapt van
gemeentebestuur.leusden.nl naar Notubiz (leusden.raadsinformatie.nl,
organisatie 2090). De openbare Notubiz-API (`api.notubiz.nl/events`,
`/events/assemblies/{id}`, `/events/meetings/{id}`) werkt zonder sleutel en
zonder Cloudflare vanaf de notebook. Het oude systeem geeft nog losse
vergaderingen (`/api/v1/meetings/{id}`), maar de lijst time-out. RaadKijker
kent Leusden (`gemeente=leusden`), zonder motienummers en uitslag.

**Resultaat:**

- `scrapers/notubiz-leusden.js` (+ `notubiz-lib.js`) in `run-all.js`: bron 162
  'Raad Leusden — vergaderstukken (Notubiz)', tier 1. Eerste run: 4
  evenementen, 8 vergaderingen, 18 documenten, alle met PDF-tekst; 9 als
  achtergrond (B&O-avonden van 10 en 17-9), 9 van de raad van 24-9 als nieuw.
- `raadkijker-moties.js` heeft een Leusden-pass: bron 163 'Raad Leusden —
  Moties en amendementen', tier 1. Eerste run: de 2 moties van 24-9, met tekst.

**Verificatie:** 4 nieuwe tests (`__tests__/notubiz/leusden.test.mjs`) plus de
bestaande RaadKijker-tests groen. Drie opeenvolgende Notubiz-runs: 18, 0, 0
nieuw (idempotent, elk ongeveer 2 seconden bij een bekende stand). In Turso
nageteld: 18 + 2 rijen, alle met fulltext, 9 historisch.

**Open:**

- Bron 129 en de ORI-Leusdenpass in `raadsinformatie-ori.js` draaien nog.
  Uitzetten of laten staan (ORI kan Leusden weer oppakken) is aan Jasper.
- Moties vreemd aan de agenda staan niet als document in de Notubiz-vergadering;
  daarom de RaadKijker-pass. De uitslag van Leusdense moties komt nog nergens
  vandaan.
- Het eerste echte `run-all`-resultaat (60 seconden-limiet) nog nakijken in
  `scrape_runs`.
